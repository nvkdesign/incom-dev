{% extends '::base.html.twig' %}
{% block body %}

<div class="container">
    <div class="row-centered">
        <div class="col-lg-12">
            <div class="col-lg-9">
                <h1 class="page-header">Admin dashboard
                    <small> Validation de l'équipe gagnante </small>
                </h1>
            </div>
            <div class="col-lg-3">
                <a href= {{ path ('event_event_admin') }}><button type="button" class="btn btn-info">Retour à la liste des paris</button></a>
            </div>
        </div>

        <div class="col-lg-12">
                <h4> " Match : {{ event.jeu.nom }} - {{ event.titre }}"</h4>
        </div>
    </div>
    <form action="{{ actionForm }}" {{ form_enctype(formValidate) }} method="POST">
    <div class="row">
        <div class="col-lg-12">

            <hr>
            <h4>{{ form_label(formValidate.equipegagnante, "Sélectionner l'équipe gagnante : ") }}</h4>
                {{ form_errors(formValidate.equipegagnante) }}
                {{ form_widget(formValidate.equipegagnante, { 'attr': {'class': 'form-control'} }) }}

        </div>
    </div>

        <div class="row">
            <div class="col-lg-12">



            </div>
        </div>

    <hr>
    <hr>
    <div class="row">
        <div class="col-lg-12">

            {{ form_widget(formValidate.valider, { 'attr': {'class': 'btn btn-primary btn-lg btn-block'} }) }}
            {{ form_rest(formValidate) }}

        </div>
    </div>
</div>
    </form>

{% endblock %}

